<?xml version="1.0" encoding="UTF-8" ?>
<Shell
    x:Class="Zadania.AppShell"
    xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
    xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
    xmlns:local="clr-namespace:Zadania"
    Title="Zadania"
    FlyoutBehavior="Flyout">

    <TabBar>
        <Tab Title=" 15.4">
            <ShellContent
                Title="Ustawienia"
                ContentTemplate="{DataTemplate local:SettingsPage}"
                Route="SettingsPage" />
        </Tab>
        <Tab Title="15.3">
            <ShellContent
                Title="Sklep"
                ContentTemplate="{DataTemplate local:Sklep}"
                Route="Sklep" />
        </Tab>
        <Tab Title="15.2">
            <ShellContent
                Title="Rejestracja"
                ContentTemplate="{DataTemplate local:Rejestracja}"
                Route="Rejestracja" />
        </Tab>
    </TabBar>

</Shell>
